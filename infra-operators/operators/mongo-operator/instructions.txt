# Apply in the following order
Apply mongo crd (kubectl apply -f ./crd.yaml)
Apply mongo-roles-bindings (kubectl apply -k ./mongo-roles-bindings/)
Apply mongo operator (kubectl apply -f ./operator.yaml)