<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0" />
        <meta http-equiv="X-UA-Compatible" content="ie=edge" />
        <title>Redirect</title>
        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;300;400;500;700;900&display=swap" rel="stylesheet" />
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
            crossorigin="anonymous" />
        <style>
            body,
            html {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
                height: 100vh;
                width: 100vw;
                max-width: 100vw;
                max-height: 100vh;
                display: flex;
                justify-content: center;
                align-items: center;
                overflow: hidden;
            }

            .spinner {
                position: relative;
                display: flex;
                justify-content: center;
                align-items: center;
                border-radius: 50%;
                height: 400px;
                width: 400px;
                transform: scale(1);
                max-width: 400px;
                max-height: 400px;
            }

            .spinner-inside {
                position: absolute;
                display: flex;
                justify-content: center;
                flex-direction: column;
                align-items: center;
                font-weight: bolder;
                border-radius: 50%;
                background-color: #fff;
                z-index: 2;
            }

            .spinner-sector {
                position: absolute;
                height: 100%;
                width: 100%;
                height: 400px;
                width: 400px;
                transform: scale(1);
                max-width: 400px;
                max-height: 400px;
                border-radius: 50%;
                border: 42px solid transparent;
                mix-blend-mode: overlay;
            }

            .spinner-color-a {
                border-top: 42px solid #579bab;
                animation: rotate 2s linear infinite;
            }

            .spinner-color-b {
                border-left: 42px solid #311a88;
                animation: rotate 2.5s linear infinite;
            }
            .spinner-color-c {
                border-right: 42px solid #ec1c24;
                animation: rotate 1.5s linear infinite;
            }

            @keyframes rotate {
                from {
                    transform: rotate(0);
                }
                to {
                    transform: rotate(360deg);
                }
            }
            @keyframes pulse {
                0% {
                    transform: scale(1);
                }
                75% {
                    transform: scale(1.1);
                }
                100% {
                    transform: scale(1);
                }
            }
            svg {
                animation: pulse infinite 1s linear;
                max-width: 150px;
            }

            .message {
                position: absolute;
                text-align: center;
                top: 10%;
            }

            @media screen and (max-width: 768px) {
                .spinner {
                    transform: scale(0.5);
                }
            }
        </style>
    </head>
    <body class="container">
        <div class="message">
            <h1 class="text-success">Successfully logged in</h1>
            <h2>Redirecting...</h2>
        </div>
        <div class="d-flex align-items-center flex-column text-center justify-content-center mt-5">
            <div class="spinner">
                <div class="spinner-inside">
                    <div class="logo"><%- include('./acme-pill.svg') %></div>
                </div>
                <div class="spinner-sector spinner-color-a"></div>
                <div class="spinner-sector spinner-color-b"></div>
                <div class="spinner-sector spinner-color-c"></div>
            </div>
        </div>

        <script
            src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
            crossorigin="anonymous"></script>
        <script>
            setTimeout(function () {
                window.location.href = "https://acmefast.dev/";
            }, 2000);
        </script>
    </body>
</html>
